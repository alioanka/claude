# Minimal Enhanced Dashboard Requirements
# Compatible with older Python versions and existing installations

# Core web framework (flexible versions)
fastapi>=0.95.0
uvicorn[standard]>=0.20.0
websockets>=10.0

# Template engine
jinja2>=3.0.0

# Static files
aiofiles>=23.0.0

# CORS support
python-multipart>=0.0.5

# Data processing (compatible versions)
pandas>=1.5.0
numpy>=1.21.0

# Database (compatible with existing installations)
psycopg2-binary>=2.9.0
sqlalchemy>=1.4.0

# Redis (optional)
redis>=4.0.0

# HTTP client
httpx>=0.24.0
aiohttp>=3.8.0

# Configuration
pydantic>=1.10.0
python-dotenv>=0.19.0
